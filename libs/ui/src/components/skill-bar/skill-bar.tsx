import React from 'react';

import './skill-bar.module.scss';

/* eslint-disable-next-line */
export interface SkillBarProps {}

export function SkillBar(props: SkillBarProps) {
  return (
    <div>
      <h1>Welcome to skill-bar!</h1>
    </div>
  );
}

export default SkillBar;
